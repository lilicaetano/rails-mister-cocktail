<div id="show-cocktail" class="show-cocktail">
  <h1><%= @cocktail.name.capitalize  %></h1>
</div>

<div class="cocktail-card-recipe">
  <%= cl_image_tag @cocktail.photo, height: 300, width: 400, crop: :fill %>
  
  <div class="cocktail-card-infos">
    <h2>Recipe</h2>
    <table class="table table-bordered">
      <tbody>
        <thead>
          <th>Quantity</th>
          <th>Ingredients</th>
        </thead>
      </tbody>
    </table>
    <p><%= link_to 'Edit', edit_cocktail_path(@cocktail) %></p>
  </div>
</div>



<!-- <div id="img-cocktail" class="img-cocktail">
  <%= cl_image_tag @cocktail.photo, height: 300, width: 400, crop: :fill %>
</div>
<ul>
<div class="row">
    <div class="col-sm-12 col-md-6">
      <h3>Recipe</h3>
      <table class="table table-bordered">
        <tbody>
          <thead>
            <th>Ingredient</th>
            <th>Description</th>
          </thead>

<%  @cocktail.doses.each do |dose| %>
  <li><%= dose.ingredient.name %></li>
  <li><%= @cocktail.ingredient.name %></li>
   <%= dose.description %> -->
  <!-- <%= link_to 'Remove ingredient', cocktail_dose_path(@cocktail, dose), method: :delete, data: { confirm: 'Are you sure?' } %> -->
<% end %>
</ul>
<br />
<br />
<!-- <p><%= link_to 'Add ingredient', new_cocktail_dose_path(@cocktail) %></p> -->
<p><%= link_to 'Back to list', cocktails_path %></p>
