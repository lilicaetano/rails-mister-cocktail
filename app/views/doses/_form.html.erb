<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :ingredient_id, collection: Ingredient.all.order(:name) %>
  <%= f.input :description %>
  <%= f.submit value: "Add", class: "btn btn-cocktails" %>
<% end %>